version: "2"
run:
  tests: true
linters:
  enable:
    - bodyclose
    - exhaustive
    - godot
    - gomoddirectives
    - goprintffuncname
    - gosec
    - misspell
    - nakedret
    - nilerr
    - noctx
    - nolintlint
    - prealloc
    - rowserrcheck
    - sqlclosecheck
    - tparallel
    - unconvert
    - unparam
    - whitespace
    # - wrapcheck
  exclusions:
    generated: lax
    presets:
      - common-false-positives
    rules:
      # httptest.NewRequest without a context is idiomatic in tests,
      # and gosec targets production code, not test helpers.
      - path: _test\.go
        linters:
          - noctx
          - gosec
issues:
  max-issues-per-linter: 0
  max-same-issues: 0
formatters:
  enable:
    - gofumpt
    - goimports
  exclusions:
    generated: lax
